server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html;
    index index.html;

    location /api {
      proxy_pass http://weather_api:9393;
    }

    location / {
      proxy_pass http://weather_ui;
    }
}
